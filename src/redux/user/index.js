/* This file is mostly a QOL thing that also lets us fulfil most of the qualification for a duck per the proposal */
import {types, actionCreators} from './userActions'
import {userReducer} from './userReducer'

export {
  types,
  actionCreators
}

export default userReducer
