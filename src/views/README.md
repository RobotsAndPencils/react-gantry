Each view should have its own subdirectory of the format

```bash
src
└── views
    └── <view>
        ├── <View>.js
        └── <view>.module.scss
```

And components specific to this view (not common) should live here as well
