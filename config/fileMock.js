/* This file will be used to describe what a particular import should return
 * when Jest attempts to import it. In this case we're just stubbing out
 * files so that jest doesn't freak out. As such we literally just return
 * the string "file" as the import.
 */

export default 'file'
